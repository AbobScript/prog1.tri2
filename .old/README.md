# prog1.tri2

- no windows
    - crie uma pasta (temporária)
    - na barra de endereço: cmd
    - então execute os comandos abaixo

    ```bash
    git clone https://<<git-token>>@github.com/<<drachehavoc/prog1.tri2.ativ1>> projeto

    ```bash
    code --user-data-dir=vscode-data projeto
    ```
```bash
    git add --all
    git commit -m "<<descrição doq foi feito em aula>>"
    git push
```